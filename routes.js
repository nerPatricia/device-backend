const express = require("express");
const routes = express.Router();

/* 
const deviceController = require("./controllers/deviceController");
const categoryController = require("./controllers/categoryController");

// get all devices
routes.get("/device", deviceController.getDevices);
// post device
routes.get("/device", deviceController.postDevice);
// delete device
routes.get("/device/:id", deviceController.deleteDevice);
// get all categories
routes.get("/category", categoryController.getCategory);
// post category
routes.get("/category", categoryController.postCategory);
// delete category
routes.get("/category/:id", categoryController.deleteCategory);
*/

module.exports = routes;    
